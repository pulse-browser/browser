diff --git a/toolkit/themes/shared/in-content/common.inc.css b/toolkit/themes/shared/in-content/common.inc.css
index 859ba3310fcaeb90785ef0a751311c0e3fa22ed0..dcd4c01b5f3abd184e91f65a95988e3d98013953 100644
--- a/toolkit/themes/shared/in-content/common.inc.css
+++ b/toolkit/themes/shared/in-content/common.inc.css
@@ -8,6 +8,17 @@
 
 :host,
 :root {
+  /* Colors */
+  --gradient-orientation: 0;
+  --gradient: linear-gradient(
+    var(--gradient-orientation),
+    hsla(212, 97%, 54%, 1) 0%,
+    hsla(295, 70%, 58%, 1) 100%
+  );
+
+  --border-radius-1: 0; /* Default: 2px */
+  --border-radius-2: 0; /* Default: 4px */
+
   --in-content-page-color: rgb(21, 20, 26);
   --in-content-page-background: #fff;
   --in-content-text-color: var(--in-content-page-color);
@@ -347,7 +358,7 @@ xul|menulist {
   min-height: 32px;
   color: var(--in-content-button-text-color);
   border: 1px solid var(--in-content-button-border-color);
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   background-color: var(--in-content-button-background);
   font-weight: 400;
   padding: 7px 15px;
@@ -541,7 +552,7 @@ xul|menulist > xul|menupopup {
   background-color: transparent;
 
   --panel-border-color: var(--in-content-box-border-color);
-  --panel-border-radius: 2px;
+  --panel-border-radius: var(--border-radius-1);
   --panel-background: var(--in-content-box-background);
   --panel-color: var(--in-content-text-color);
   --panel-padding: 0;
@@ -590,7 +601,7 @@ html|textarea,
 xul|search-textbox {
   appearance: none;
   border: 1px solid var(--in-content-box-border-color);
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   color: inherit;
   background-color: var(--in-content-box-background);
 }
@@ -661,7 +672,7 @@ html|a:-moz-focusring,
 .text-link:-moz-focusring {
   outline: 2px solid var(--in-content-focus-outline-color);
   outline-offset: 1px;
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
 }
 
 button.text-link {
@@ -687,7 +698,7 @@ html|input[type="checkbox"] {
   width: 16px;
   border: 1px solid var(--checkbox-border-color);
   background-color: var(--checkbox-unchecked-bgcolor);
-  border-radius: 2px;
+  border-radius: var(--border-radius-1);
   margin-inline: 0 6px;
   flex-shrink: 0; /* avoid shrinking inside flex container */
 }
@@ -854,7 +865,7 @@ html|*#categories {
 }
 
 #categories > .category {
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   min-height: 48px;
   appearance: none;
   color: inherit;
@@ -1015,7 +1026,7 @@ xul|richlistbox {
   margin-inline: 0;
   background-color: var(--in-content-box-background);
   border: 1px solid var(--in-content-box-border-color);
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   color: var(--in-content-text-color);
 }
 
@@ -1080,7 +1091,7 @@ xul|richlistitem[selected] xul|menulist:focus-visible {
 @media (prefers-contrast) {
   xul|treechildren::-moz-tree-row(selected) {
      border: 2px solid currentColor;
-     border-radius: 4px;
+     border-radius: var(--border-radius-2);
   }
 }
 
@@ -1096,7 +1107,7 @@ xul|tree {
   appearance: none;
   font-size: 1em;
   border: 1px solid var(--in-content-box-border-color);
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   background-color: var(--in-content-box-background);
   color: inherit;
   margin: 0;
@@ -1171,7 +1182,7 @@ xul|treechildren::-moz-tree-image(selected) {
 /* Message bars */
 .message-bar {
   background-color: var(--in-content-box-info-background);
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   min-height: 32px;
   -moz-box-align: center;
   padding: 4px;
@@ -1192,7 +1203,7 @@ xul|treechildren::-moz-tree-image(selected) {
 .message-bar-content > .message-bar-button {
   background-color: var(--grey-90-a10);
   border: none;
-  border-radius: 2px;
+  border-radius: var(--border-radius-1);
   height: 24px;
   margin-inline-start: 8px;
   padding: 0 8px;
@@ -1237,7 +1248,7 @@ input[type="text"][warning]:enabled:not(:focus) {
   background: var(--in-content-box-background);
   /* Needed for high-contrast where the border will appear. */
   border: 1px solid transparent;
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   box-shadow: var(--card-shadow);
   margin: 0 0 8px;
   /* Remove the border from the overall padding. */
@@ -1271,7 +1282,7 @@ input[type="text"][warning]:enabled:not(:focus) {
 .sidebar-footer-link {
   height: 36px;
   cursor: default;
-  border-radius: 4px;
+  border-radius: var(--border-radius-2);
   display: flex;
   align-items: center;
 }
